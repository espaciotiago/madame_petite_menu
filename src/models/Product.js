class Product {
  constructor(name, label, description, images, price) {
    this.name = name;
    this.label = label;
    this.description = description;
    this.images = images;
    this.price = price;
  }
}

export default Product;
